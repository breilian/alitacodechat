import { forwardRef } from "react";

const ChatBox = forwardRef((props, boxRef) => {
  return <div>ChatBox Update 2</div>
});

ChatBox.displayName = 'ChatBox'

ChatBox.propTypes = {}


export default ChatBox;